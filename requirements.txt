# Core Dependencies
openai>=1.50.0             # xAI Grok API (OpenAI-compatible)
anthropic>=0.45.0          # Claude API for collaboration mode
tenacity>=8.2.0            # Retry logic with exponential backoff (CRITICAL for Phase 0)
psutil>=5.9.0              # System performance monitoring (for analytics tools)
redis>=4.5.0               # Redis client for learning persistence
pyautogui>=0.9.54          # Screen capture and control
pillow>=10.0.0             # Image processing
requests>=2.31.0           # HTTP requests for web tasks
python-dotenv>=1.0.0       # Environment variable management
pydantic>=2.8.0            # Data validation for message models

# Agent & LLM Stack
langchain>=0.1.0           # Agent workflows and memory
langgraph>=0.1.0           # State machines for multi-agent orchestration
autogen>=0.2.0             # Multi-agent conversations and tool use
crewai>=0.1.0              # Role-based agent teams
llama-index>=0.9.0         # RAG pipelines and data connectors
chromadb>=0.4.0            # Vector database for agent memory
sentence-transformers>=2.2.0 # Text embeddings for RAG

# CLI
click>=8.1.0               # Command-line interface

# MCP Server
flask>=2.0.0               # Web framework for MCP API

# Utilities
typing-extensions>=4.8.0   # Type hints for older Python versions
numpy>=1.21.0              # Numerical computing for cognitive enhancements

# Local Models
ollama>=0.3.0              # Local Ollama models

# Google Gemini
google-generativeai>=0.8.0 # Google Gemini AI models

# Vision-Language Models & Fine-tuning
transformers>=4.40.0       # Hugging Face transformers for VLMs
torch>=2.0.0               # PyTorch for model training
datasets>=2.18.0           # Hugging Face datasets for training data
accelerate>=0.28.0         # Training acceleration
peft>=0.10.0               # Parameter-efficient fine-tuning

# Computer Vision
opencv-python>=4.8.0       # OpenCV for computer vision tasks

# LoRA Fine-Tuning Dependencies (AMD ROCm compatible)
# Install PyTorch with ROCm first: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.1
bitsandbytes>=0.41.0       # 4-bit quantization (Note: May need special build for AMD)
safetensors>=0.4.0         # Safe model serialization
sentencepiece>=0.1.99      # Tokenizer support
trl>=0.7.0                 # Transformer Reinforcement Learning (SFTTrainer)

# Development Dependencies
pytest>=7.4.0              # Testing framework
pytest-cov>=4.1.0          # Coverage reporting
pytest-asyncio>=0.23.0     # Async testing support
black>=23.0.0              # Code formatting
flake8>=6.1.0              # Linting
jsonschema>=4.21.0         # JSON schema validation
streamlit>=1.32.0          # Web interface
fastmcp>=0.1.0             # Fast MCP server
